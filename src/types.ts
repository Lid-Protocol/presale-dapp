export interface DappMetaData {
  tokenName: string;
  tokenSymbol?: string;
  tokenOwnerWebsite?: string;
  siteUrl?: string;
  totalPresale: string;
  referralBP: string;
  basisPoint: string;
  accountCap: string;
}
